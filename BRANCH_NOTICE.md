# Active Development Branch

All active development happens on **`master`**, not `main`.

```
git checkout master
git pull origin master
```

Do not commit to `main` — it exists only as a legacy default.
